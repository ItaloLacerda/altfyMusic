import { createTheme } from '@mui/material';

const theme = createTheme({
  typography: {
    h1: {
      fontSize: '40px',
      fontWeight: '700',
      color: '#FFFFFF',
    },
  },
});

export default theme;
